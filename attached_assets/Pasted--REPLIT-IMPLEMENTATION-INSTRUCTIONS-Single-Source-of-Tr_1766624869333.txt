/**
 * REPLIT IMPLEMENTATION INSTRUCTIONS (Single Source of Truth)
 * Candidate Command Center landing page: clarity + pop updates
 *
 * What we are implementing (locked decisions):
 * 1) Keep the input helper EXACTLY as-is:
 *    "Searches across LinkedIn, GitHub, blogs, portfolios, and live professional activity."
 *
 * 2) Add Option A novelty line under the hero subline (one sentence only):
 *    "Powered by a new generation of internet-scale people search, now applied to recruiting."
 *
 * 3) Place example queries IMMEDIATELY under the search input (inside Hero),
 *    as clickable chips that auto-fill the input.
 *
 * 4) Keep example queries limited to 3–4 max, no explanations per chip, no quotes.
 *
 * 5) Do NOT add more “newness” messaging elsewhere on the page.
 *
 * ---------------------------------------------
 * WHERE TO EDIT (Replit)
 * - If you have a Hero component:
 *     src/components/Hero.jsx (or .tsx)
 * - If everything is in one page:
 *     src/App.jsx or src/pages/Home.jsx
 *
 * ---------------------------------------------
 * HERO SECTION ORDER (must match)
 * 1. Headline
 * 2. Subline (output-focused)
 * 3. Option A novelty line (one sentence)
 * 4. Search input
 * 5. Example query chips (right under input)
 * 6. CTA button
 * 7. Input helper (unchanged, sits under CTA or under input depending on your design)
 *
 * ---------------------------------------------
 * BEHAVIOR RULES
 * - Input remains editable.
 * - Clicking a chip should ONLY fill the input (no auto-search unless you choose).
 * - Chip click calls setQuery(exampleString).
 *
 * ---------------------------------------------
 * STYLE RULES (Tailwind suggestions)
 * - Novelty line: text-sm font-medium text-slate-600 (subtle, not shouty)
 * - Chip label: text-xs font-medium text-slate-500
 * - Chips: rounded-full border border-slate-200 bg-white text-xs text-slate-700 hover:border-slate-900
 * - Spacing: mt-3 under input; flex-wrap gap-2
 *
 * ---------------------------------------------
 * DROP-IN HERO JSX (copy/paste)
 * - Replace your existing Hero with this or merge the marked blocks into yours.
 */

import React, { useMemo, useState } from "react";

export default function Hero() {
  const [query, setQuery] = useState("");

  const examples = useMemo(
    () => [
      "Senior engineers in SF who’ve spoken at React conferences",
      "Product managers at fintech companies who write about design systems",
      "Directors of engineering who contribute to open source",
      "ML engineers who publish papers or technical blogs",
    ],
    []
  );

  const handleSearch = (q = query) => {
    // TODO: Wire to your route/API:
    // navigate(`/search?q=${encodeURIComponent(q)}`)
    console.log("Search:", q);
  };

  return (
    <section className="mx-auto max-w-3xl px-5 pb-14 pt-10">
      {/* 1) Headline */}
      <h1 className="text-4xl font-semibold leading-tight tracking-tight text-slate-900">
        From the entire internet to interview-ready candidates in under 2 minutes.
      </h1>

      {/* 2) Subline (keep tight; describe output) */}
      <p className="mt-4 text-lg leading-relaxed text-slate-600">
        Get ranked, interview-ready candidates with a clear reason for every match.
      </p>

      {/* 3) Option A novelty line (single sentence only) */}
      <p className="mt-2 text-sm font-medium text-slate-600">
        Powered by a new generation of internet-scale people search, now applied to recruiting.
      </p>

      <div className="mt-7">
        <div className="rounded-2xl border border-slate-200 bg-white p-3 shadow-sm">
          {/* 4) Search input */}
          <input
            value={query}
            onChange={(e) => setQuery(e.target.value)}
            placeholder="Describe the person you need, not keywords"
            className="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm text-slate-900 placeholder:text-slate-400 focus:border-slate-900 focus:outline-none"
          />

          {/* 5) Example queries (must be directly under input) */}
          <div className="mt-3">
            <p className="mb-2 text-xs font-medium text-slate-500">Try searches like:</p>

            <div className="flex flex-wrap gap-2">
              {examples.map((q) => (
                <button
                  key={q}
                  type="button"
                  onClick={() => setQuery(q)}
                  className="rounded-full border border-slate-200 bg-white px-3 py-1.5 text-xs text-slate-700 hover:border-slate-900"
                >
                  {q}
                </button>
              ))}
            </div>
          </div>

          {/* 6) CTA */}
          <button
            type="button"
            onClick={() => handleSearch()}
            className="mt-4 w-full rounded-xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white hover:bg-slate-800"
          >
            Find Candidates Now →
          </button>

          {/* 7) Input helper (UNCHANGED — do not edit) */}
          <p className="mt-3 text-center text-xs text-slate-500">
            Searches across LinkedIn, GitHub, blogs, portfolios, and live professional activity.
          </p>
        </div>
      </div>
    </section>
  );
}

/**
 * OPTIONAL (do only if you want chip click to also run a search immediately)
 *
 * Replace:
 *   onClick={() => setQuery(q)}
 * With:
 *   onClick={() => { setQuery(q); setTimeout(() => handleSearch(q), 0); }}
 *
 * Keep everything else the same.
 */
